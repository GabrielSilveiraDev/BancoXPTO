<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a id="manage" class="nav-link" href="#" title="Manage">Olá @((await UserManager.GetUserAsync(User)).Nome.Split(" ")[0]) @((await UserManager.GetUserAsync(User)).Nome.Split(" ")[1])</a>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link">Sair</button>
        </form>
    </li>
}

</ul>
